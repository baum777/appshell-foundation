# TradeApp Environment Variables

# ─────────────────────────────────────────────────────────────
# FRONTEND (Vite)
# ─────────────────────────────────────────────────────────────

# API Configuration
VITE_API_URL=http://localhost:3000/api

# Feature Flags
VITE_ENABLE_DEV_NAV=false
VITE_ENABLE_ANALYTICS=false

# Application Version
VITE_APP_VERSION=0.1.0

# Sentry (Optional)
# VITE_SENTRY_DSN=

# Analytics (Optional)
# VITE_ANALYTICS_ID=

# ─────────────────────────────────────────────────────────────
# BACKEND (Legacy - for local dev server, not used in Vercel)
# ─────────────────────────────────────────────────────────────

# Environment
NODE_ENV=development

# Server (legacy, not used in Vercel deployment)
BACKEND_PORT=3000
API_BASE_PATH=/api

# Database (SQLite - legacy, not used in Vercel deployment)
DATABASE_URL=sqlite:./.data/tradeapp.sqlite

# Logging
LOG_LEVEL=info

# ─────────────────────────────────────────────────────────────
# VERCEL API (Serverless Functions)
# ─────────────────────────────────────────────────────────────

# Vercel KV (Required for production)
# Get these from your Vercel project settings
# KV_REST_API_URL=https://your-kv.upstash.io
# KV_REST_API_TOKEN=your-token
# KV_REST_API_READ_ONLY_TOKEN=your-read-only-token

# External Providers (Optional - stubs used if missing)
# DEXPAPRIKA_API_KEY=
# MORALIS_API_KEY=

# AI Provider (Optional - stubs used if missing)
# OPENAI_API_KEY=

# Cron Security (Optional but recommended for production)
# CRON_SECRET=your-secret-for-cron-auth
